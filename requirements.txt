# Phase 0 — data generation
openai>=1.30.0
python-dotenv>=1.0.0
tenacity>=8.2.0
tqdm>=4.66.0

# Phase 1 — indexing + retrieval
rank-bm25>=0.2.2
sentence-transformers>=2.7.0
faiss-cpu>=1.8.0
numpy>=1.26.0

# Phase 3 — feature builder (parquet I/O + dataframes)
pandas>=2.2.0
pyarrow>=16.0.0

# Phase 4 — LightGBM ranker
lightgbm>=4.3.0
joblib>=1.4.0
